#
#   demo: apcomp integer 1 sum
#
def apcomp : {nth (nth 2:A) : get (nth 1:A) : B}

def by_component : { (first:A) :
                      ((last:A) n : ap {first : get (first:A):B} A : B)
                      ((last:A) n : ap {last  : get (first:A):B} A : B)
                   }

  X : nth 1 : get integer : B
  X : nth 2 : get integer : B
